function logPets() {
	var pet = 'cat'
	console.log(pet)
	pet = 'dog'
	console.log(pet)
}

logPets();

// Write a function that adds three numbers


function Add(num1, num2, num3) {
	console.log(num1 + num2 + num3);
}

Add(1, 2, 3);

// Write a function that adds three numbers and divide result by 3

function Average(num1, num2, num3) {
	console.log((num1+num2+num3)/3);
}

Average(1, 2, 3);